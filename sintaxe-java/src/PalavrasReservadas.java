public class PalavrasReservadas {
    public static void main(String[] args) {
        // Existem ao todo 52 palavras reservadas, e são todas em minúsculas
        // CONTROLE DE PACOTES: import, package
        // MODIFICADORES DE ACESSO: puiblic, private, protected
        // PRIMITIVOS: boolean, byte, char ,double, float, int, long, short, void
        // (ausência de tipo/valor)
        // MODIFICADORES DE CLASSES, MÉTODOS OU VARIÁVEIS: abstract, class, extends,
        // final, implements, interface, native (o método está escrito em linguagem que
        // depende da plataforma), new, static(faz um método ou variável pertencer à
        // classe ao invés de às instâncias), stritctfp(usado em frente a método ou
        // classe, para indicar que seguiram as regras de ponto flutuante), synchronized
        // (só pode ser acessado por uma thread de cada vez), transient (impede a
        // serialização de campos para que a informação seja sempre reinicia e não será
        // saldo em registro), volatile (uma variável pode ser alterada durante o uso de
        // threads)
        // CONTROLE DE FLUXO: break, case, continue
        // PLAVRAS OPOSTAS: package e import, extends e implements, final e abstract,
        // throws e throw
    }
}
